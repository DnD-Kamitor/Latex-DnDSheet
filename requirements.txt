# Core dependencies
# Note: These match the versions in pyproject.toml

# Template engine for LaTeX generation
jinja2>=3.1.0

# YAML support for character data files
pyyaml>=6.0.0

# TUI dependencies (required for TUI features)
textual>=0.47.0
rich>=13.0.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
